# Declarative pattern adoption inventory

_Generated by hand for slice `wiring.declarative_pattern_adoption_audit` (31). Each row maps an imperative control-plane surface to a migration decision and owner._

**Adoption rule (recap).** YAML/JSON-schema files describe **control-plane intent**; Rust/TypeScript runtime code holds **runtime authority**. No YAML may grant runtime power beyond what Rust/TS enforces.

## 1. Command catalogs

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Canonical command manifest | `config/control-plane/command-manifest.yaml` | Declarative YAML, source of truth. | Keep declarative. Generate Rust/TS bindings. | bridge + protocol |
| Generated Rust catalog | `apps/local-bridge/src/generated/command_catalog.rs` | Generated from manifest by `scripts/codegen-command-catalog.mjs`. | Keep generated. Drift CI gate enforced. | protocol |
| Generated TS catalog | `apps/web/src/generated/commandCatalog.ts` | Generated from manifest. | Keep generated. Drift CI gate enforced. | protocol |
| ACP request senders | `apps/web/src/actions/*` | Imperative TS that maps user intent to command IDs. | Frontend-owned; bind to generated `commandCatalog.ts`. | web |
| Slash palette commands | `apps/web/src/components/Composer/*` | Imperative array of slash items. | Bind to `commandCatalog.ts` `frontend_owned` entries. | web |

## 2. Event taxonomy

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Translator emit sites | `apps/local-bridge/src/translator/mod.rs` | Imperative `emit_notification("...")` literals. | Migrate to generated event-id constants (slice 32). | bridge |
| Notify error codes | `apps/local-bridge/src/notify*.rs` | Imperative string codes. | Migrate to generated event-id constants. | bridge |
| Profile denial codes | `packages/profile-core/src/enforce.rs` | Imperative string codes. | Migrate to generated profile-denial enum. | bridge |
| Web UX classifiers | `apps/web/src/domain/capabilities/*` | Hand-rolled classifier modules covering profile/wire/handoff/session/notify/etc. | Keep declarative TS; consume generated event-id constants once available. | web |

## 3. Workflow specs

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Local-bridge workflow runtime | `apps/local-bridge/src/workflows/*` | Imperative Rust. | Keep runtime authority in Rust; declarative spec lives in `examples/workflows/*.yaml` (slice 35). | bridge |
| Workflow YAML examples | `examples/workflows/*.yaml` | TBD (slice 35). | Author declarative spec first, runtime second. | product + bridge |

## 4. Mock scenarios

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Mock-engine scenarios | `tools/mock-engine/src/scenarios.rs` | Imperative Rust scenario code. | Migrate to declarative YAML scenarios (slice 34). Catalogued via `NOTIFICATION_METHOD_CATALOG` for parity. | tools |
| Mock notification parity catalog | `tools/mock-engine/src/scenarios.rs::NOTIFICATION_METHOD_CATALOG` | Declarative `&[(&str, NotificationParity)]` slice. | Keep as adapter until YAML scenarios land. | tools |

## 5. UI command senders / event listeners

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Topbar | `apps/web/src/components/cockpit/Topbar.tsx` | Imperative event handling. | Frontend-owned; consume capability modules and `commandCatalog.ts`. | web |
| Toast / Notify lane | `apps/web/src/domain/notify` | Imperative classification. | Replace with `notifyClass` + `notifyAttention` modules. | web |
| Session picker | `apps/web/src/components/SessionPicker` | Imperative event handling. | Frontend-owned; consume `sessionLifecycle` module. | web |
| Review / file diff | `apps/web/src/domain/review/handlers.ts` | Imperative review handlers. | Frontend-owned; consume review taxonomy module. | web |

## 6. Agent / registry / config profiles

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Capability profiles | `docs/capability-profiles.md` + `config/profiles/*.yaml` | Declarative YAML. | Keep declarative. | bridge |
| Registry/MCP config | `config/registry/*.yaml` | Declarative YAML. | Keep declarative. Validate against schema. | bridge |

## 7. Implementation slice plans

| Surface | Location | Today | Decision | Owner |
| --- | --- | --- | --- | --- |
| Wiring slice plans | `docs/plans/wiring/*.md` | YAML-in-Markdown declarative slices. | Keep declarative. Each new slice ships YAML control-plane block + acceptance + validation gates. | product + bridge + web |

## Acceptance check

* Every imperative command/event catalog above has a migration decision. ✓
* No YAML file in this inventory grants runtime power beyond Rust/TS enforcement. ✓
* Implementation tasks per row are small and mechanically actionable. ✓

## Open follow-ups

1. Slice 32: generate event-id constants from a YAML event catalog and wire them into translator + UX classifier modules.
2. Slice 34: port `tools/mock-engine/src/scenarios.rs` to YAML scenarios with schema validation.
3. Slice 35: ship `examples/workflows/assess-index-rebuild.yaml` plus the workflow-authoring template doc.
4. Slice 33: bind Topbar / SessionPicker / Toast / transcript surfaces to the new capability modules so each one renders from declarative copy.
