schema_version: 1
kind: capability
id: bridge.audit
title: Audit
status: ready
owner:
  crate: local-bridge
  module: audit
description: Appends raw tool-call decisions, redacts sensitive arguments, and rotates files.
policy:
  risk: local_mutation
  mutates_files: true
  network: false
