# Frozen protocol snapshots

This directory will contain versioned frozen copies of `packages/protocol/v1/` at release milestones. Current development schemas live in `packages/protocol/v1/`; snapshots here are immutable artefacts for reproducibility.

First snapshot target: end of Phase 5 (v1.0.0).

Until then, empty.
