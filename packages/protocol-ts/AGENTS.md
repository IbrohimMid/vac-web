# packages/protocol-ts

TypeScript generated protocol types. **Read-only — generated by `tools/codegen`.**

## Do NOT edit

- Files under `src/v1/generated/` are codegen output. Edit the schema and re-run `bash scripts/codegen.sh`.
- `dist/` is build output (`pnpm -r build`).
- The header comment is verified by drift CI; missing or mutated headers fail `verify-codegen.sh`.

## Tests

- `pnpm --filter @vac-web/protocol-ts test`.
